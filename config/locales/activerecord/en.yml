en:
  activerecord:
    models:
      event: "Event"

    attributes:
      event:
        title: "Title"

    errors:
      messages:
        required: "Required field"
        blank: "Can't be empty"
